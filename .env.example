# 값은 비워 둔다. 비밀번호는 평문으로 두지 않는다.
# 설정: npm run admin:password -- 새비밀번호
# NEXT_PUBLIC_ 접두어를 붙이지 않는다.

ADMIN_PASSWORD_HASH=
ADMIN_SESSION_SECRET=
ADMIN_SESSION_EPOCH=0

# 문답표 배포 저장. 로컬만 쓸 때는 비워 둔다.
# NEXT_PUBLIC_ 접두어를 붙이지 않는다. 키는 채팅에 붙여 넣지 않는다.
CHECK_STORE=
SUPABASE_URL=
SUPABASE_SECRET_KEY=
